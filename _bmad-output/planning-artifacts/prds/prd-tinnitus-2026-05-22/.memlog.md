---
topic: FormForge (tinnitus) PRD
updated: 2026-09-08T09:04
---

- (event by bootstrap) reconstructed from prd.md + .decision-log.md (57 rows, 2026-05-22 to 2026-06-03) — pre-memlog project; this is a thin summary, not 1:1
- (decision) Auth: JWT Bearer, access token in-memory, refresh token in HttpOnly SameSite=Strict cookie, silent re-auth on reload (OQ-5)
- (decision) Tenancy: Single-tenant — simpler data model, stated requirement; multi-tenant considered and rejected at the time
- (decision) Menu hierarchy capped at 2 levels; soft delete via `is_deleted` on all dynamic tables, all records returned by default, Platform Admin can restore (OQ-2/OQ-3)
- (assumption) .NET 10 LTS, React Router v7 (later overridden to TanStack Router by architecture), menu binding pinned to {designerId, version}, Repeater FK ON DELETE CASCADE for hard deletes only, no soft-delete child cascade at PRD time (later resolved: cascades, OQ-2), bulk import/export and full-text search out of scope v1, i18n architecture-ready English-only, no CSRF (no cookie auth for access token)
- (event) PRD finalized as draft 2026-05-22 (product name FormForge, OQ-1); no admin impersonation in v1 (OQ-6)
- (decision by user) 2026-05-31: added FR-50..53 (welcome email, forgot password, authenticated password change, TOTP MFA) to Epic A; removed old "admin resets manually" non-goal
- (decision) TOTP MFA voluntary per-user, no platform-wide enforcement (OQ-7 opened then resolved as voluntary); password reset token 32-byte random/SHA-256 hash/1hr TTL/single-use; MFA login two-step via mfaSessionToken (5-min TTL); email transport async fire-and-forget, non-blocking
- (decision by user) 2026-06-02: added FR-54 Component Mode (VIEW/CRUD) + Story B-10; mode required at creation, stored on component_schemas.mode, NOT NULL, immutable, legacy backfilled to CRUD
- (decision) VIEW-mode components never provision a table, expose no /api/data endpoints, render read-only, governed solely by Menu allowedRoles (CRUD flags not evaluated); Dropdown/Repeater component-source pickers filter to CRUD-mode only
- (question) OQ-9 opened: future VIEW→CRUD mode mutability — deferred, mode fixed at creation in v1
- (decision by user) 2026-06-03: added Dataset Manager (Epics H-K, FR-55..73) as brownfield enhancement — full scope provided upfront (data model, RBAC, name constraint, dual authoring modes, view lifecycle, NFRs)
- (decision) Dataset view lifecycle: CREATE/REPLACE/DROP VIEW runs in the same transaction as the row write; rename via ALTER VIEW RENAME preferred over DROP+CREATE (AD-17); dataset-management is a new named permission, read open to all authenticated users
- (decision) SELECT-only enforcement via SQL AST parser (not regex), CTEs permitted; optimistic concurrency via integer version column; Query Builder SQL is server-authoritative, client never sends raw SQL in Builder Mode
- (decision) Table allowlist server-side configured in v1, admin UI management deferred (OQ-11); calculated-column expression injection risk accepted, mitigated by SELECT-only + allowlist + dataset-management gate (v2: sandboxing, AD-19); dataset versioning/snapshot history out of scope v1; Datasets not bindable to Menu Items in v1 (OQ-10 opened for v2)
- (assumption) A19: Table Allowlist is server-side configured; no per-user table access control (this becomes stale once multi-tenancy lands — see 2026-09-08 entries below)
- (note) Non-Goals (§5) included: "Single-tenant deployment" (A10) and "Real-time collaboration, native apps, multi-tenant — not planned"; NFR-15 read "Single-tenant, internal-users-only, ≤100k rows/table target"
- (event) memlog bootstrap complete; live entries for the 2026-09-08 multi-tenant amendment follow below via normal append
- (decision by user) 2026-09-08: reverse Decision Log #3 (single-tenant) per approved Sprint Change Proposal (sprint-change-proposal-2026-09-08.md) — supersedes single-tenant deployment model
- (decision) Tenancy: multi-tenant, schema-per-tenant isolation, JWT-claim tenant routing (not subdomain), admin-provisioned onboarding (no self-service signup, no custom domains, no billing tiers this phase) — rationale: lowest cross-tenant-leak risk given existing Dataset Custom Query raw-SQL surface; see proposal section 2.3 and architecture.md Decisions 7.1-7.10
- (change) Non-Goals (section 5): remove A10 'Single-tenant deployment' and the 'multi-tenant not planned' line; add: self-service tenant signup, per-tenant custom domains/subdomains, per-tenant billing/plan tiers, cross-tenant data sharing or reporting
- (change) NFR-15 (Scale Target): 'Single-tenant, internal-users-only, ~100k rows/table target' becomes 'Multi-tenant (admin-provisioned tenants), schema-per-tenant isolation, ~100k rows/table target per tenant'
- (change) Assumption A19 revised: Table Allowlist and Dataset Custom Query are scoped to the requesting user's tenant schema; no cross-tenant table access is possible regardless of allowlist configuration -- was: no per-user table access control
- (decision) New Tenant Foundation and Provisioning epic added, FR-74..79: tenant data model, admin-provisioned tenant creation with tenant-admin seeding, JWT tenantId claim, platform-super-admin vs tenant-admin role split, request-scoped tenant context enforcement, minimal Tenants admin UI. Feeds architecture.md Decisions 7.1-7.10
- (note) FR-1..73 and their epics are not rewritten -- isolation under the new tenant model is structural (schema-level, architecture Decisions 7.5-7.9), not a change to existing functional behavior. FR/decision numbering stays stable; this is an additive amendment matching the FR-50..53 / FR-54 / FR-55..73 precedent
- (assumption) Multi-tenant scope for this phase confirmed by user: JWT-claim routing over subdomain; admin-provisioned onboarding over self-service signup (both recommended options in the correct-course session)
- (change) addendum.md: added 'Options Considered: Multi-Tenant Isolation Model' section (schema-per-tenant vs tenant_id rationale, JWT-claim vs subdomain routing rationale)
- (event) PRD amendments applied to prd.md: glossary terms (Tenant, Tenant Schema, Tenant Context, Platform-Super-Admin, Tenant-Admin), new Epic T (FR-74..79, Stories T-1..T-6) inserted before Epic A, Non-Goals updated, MVP Scope in/out-of-scope updated, Assumptions A10/A19 revised + A24/A25 added, Risk Register R-18..R-20 added, dependency map + sprint sequencing updated with S0.5
- (event) PRD multi-tenant amendment finalized 2026-09-08; frontmatter status left as 'draft' (consistent with prior amendment cycles for this living PRD, e.g. FR-50..53/FR-54/FR-55..73), updated date bumped; no new Open Questions introduced (isolation model and onboarding fully decided per sprint-change-proposal); reviewer/polish pass skipped by user choice
